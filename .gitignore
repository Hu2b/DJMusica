# Afhankelijkheden (worden via npm install opgehaald)
node_modules/

# Testresultaten / coverage / testrapporten (worden bij elke run opnieuw gegenereerd)
coverage/
reports/

# Logs en tijdelijke bestanden
*.log
.DS_Store
.env
.env.*
